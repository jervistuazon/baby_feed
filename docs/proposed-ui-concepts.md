# Proposed UI concepts prompt

This document records the requested `gpt-image-2` prompt for a raster concept board covering three Anya Baby Tracker UI improvements. Binary generated image files are intentionally not committed to the repository.

## Task summary

The UI concept image request is documented as text only, including the three proposed concepts and the exact prompt used for generation. Generated raster mockups should be stored outside version control, so no binary image files are committed with this task.

## Concepts included

1. **Scan-friendly timeline** — grouped day sections, last-milk status, feed-soon warning, and a clear current-event action.
2. **Smarter daily summary** — time since last milk, next suggested feed, daily totals, and shared tracker sync status.
3. **Improved entry modal** — bottom-sheet editing, selected milk/pee/poop choices, note field, delete action, and save action.

## GPT Image 2 prompt requested

```text
Use case: ui-mockup. Asset type: polished mobile app UI concept sheet. Primary request: Generate one clean raster image showing three proposed Anya Baby Tracker mobile UI concepts side by side: 1 scan-friendly timeline with grouped day sections, current slot highlight and last milk status; 2 smarter daily summary dashboard with time since last milk, next suggested feed, milk/pee/poop/notes totals, and shared sync status; 3 improved bottom-sheet entry modal with milk/pee/poop choices, note field, delete and save actions. Style: soft baby-care app, modern rounded iOS cards, white/pink/blue/purple palette, high-fidelity product design mockup. Composition: landscape concept board, three phone screens, readable but not tiny UI text. Constraints: no logos, no watermarks.
```

## Regenerating the mockup

Generate the raster mockup outside the repository or save it to an ignored local path so the repo stays text-only.
